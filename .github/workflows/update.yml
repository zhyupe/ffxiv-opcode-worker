name: Update opcodes

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Download opcode file
        run: wget ${{ secrets.OPCODE_SHEET_URL }} -O cn-opcodes.csv > /dev/null

      - name: Add & Commit
        uses: EndBug/add-and-commit@v9.0.1
