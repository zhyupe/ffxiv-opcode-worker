name: Update opcodes

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Download opcode file
        run: curl ${{ secrets.OPCODE_SHEET_URL }} > cn-opcodes.csv

      - name: Add & Commit
        uses: EndBug/add-and-commit@v9.0.1
